<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WPFPlexCastEditor"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="WPFPlexCastEditor.MainWindow"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <local:ActorCollection x:Key="ActorList"/>
    </Window.Resources>
    <Grid DataContext="{Binding Source={StaticResource ActorList}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="5">
            <Label>Select a library.</Label>
            <ComboBox x:Name="cmbLibrarySections" DisplayMemberPath="name" SelectedValuePath="id" />
        </StackPanel>
        <ListView Grid.Row="1" Name="lvThumbnails" HorizontalAlignment="Stretch" VerticalAlignment="Top" Height="200" ScrollViewer.HorizontalScrollBarVisibility="Visible" ScrollViewer.VerticalScrollBarVisibility="Visible">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical">
                        <Image Source="{Binding thumbnail}" VerticalAlignment="Top" HorizontalAlignment="Center" Stretch="UniformToFill" MaxWidth="100" MaxHeight="100"/>
                        <TextBlock Text="{Binding title}" VerticalAlignment="Bottom" HorizontalAlignment="Center" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <DataGrid Grid.Row="2" d:LayoutOverrides="Width" Name="actorsDataGrid"
                  AutoGenerateColumns="False" ItemsSource="{Binding}" 
                  SelectionMode="Extended" ColumnWidth="*" AllowDrop="True" >
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding id}" Header="Id"></DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding tag}" Header="Name"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
